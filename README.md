# ZeekAttackMonitor
This bash script automates a cybersecurity lab scenario: It launches Zeek with brute-force detection policies, triggers a remote Hydra attack from a Kali Linux instance, and automatically pushes the resulting security logs to GitHub.
